package com.assignment1.m7;

// Interface for rotating shapes
public interface Rotate {
    // Method to rotate the shape by 90 degrees
    void rotate90();

    // Method to rotate the shape by 180 degrees
    void rotate180();

    // Method to rotate the shape by a specified number of degrees
    void rotate(double degree);
}
